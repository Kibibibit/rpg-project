@abstract
extends EnumBase
class_name Element


enum Type {
	SLASH,
	BASH,
	FIRE,
	ICE
}

const SLASH := Type.SLASH
const BASH := Type.BASH
const FIRE := Type.FIRE
const ICE := Type.ICE

const VALUES: Array[Type] = [
	SLASH,
	BASH,
	FIRE,
	ICE
]
