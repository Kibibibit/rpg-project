## ENUM FILE
@abstract
extends EnumBase
class_name Element


enum Type {
	SLASH,
	BLUNT,
	PIERCE,
	FIRE,
	ICE,
	AIR,
	EARTH,
	TRUE
}

const SLASH := Type.SLASH
const BLUNT := Type.BLUNT
const PIERCE := Type.PIERCE
const FIRE := Type.FIRE
const ICE := Type.ICE
const AIR := Type.AIR
const EARTH := Type.EARTH
const TRUE := Type.TRUE

const VALUES: Array[Type] = [
	SLASH,
	BLUNT,
	PIERCE,
	FIRE,
	ICE,
	AIR,
	EARTH,
	TRUE
]
